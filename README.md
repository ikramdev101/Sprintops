"# SprintOps  Application web de gestion de projets destin‚e aux ‚quipes.  ## Structure du projet  - `/backend` - API Laravel (branche 'backend') - `/frontend` - Application React (branche 'frontend')  ## Installation  ### Backend \`\`\`bash git clone -b backend https://github.com/YOUR_USERNAME/sprintops.git sprintops-backend cd sprintops-backend composer install cp .env.example .env php artisan key:generate php artisan migrate php artisan serve \`\`\`  ### Frontend \`\`\`bash git clone -b frontend https://github.com/ikram/sprintops.git sprintops-frontend cd sprintops-frontend npm install npm start \`\`\` " 
